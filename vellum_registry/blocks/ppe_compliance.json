{
  "_populated_for": "TESTS SPATIAL GEOMETRY WITHOUT STATE. Two boxes of different classes, containment test. 1 frame.",
  "name": "ppe_compliance",
  "description": "Emit when a person has no helmet box overlapping their head region.",
  "requires": ["region.person"],
  "requires_fields": ["bbox", "class_id", "track_id"],
  "frames": "1",
  "state": "none",
  "libraries": [],
  "emits": "event.usecase",
  "provides": ["track_ids", "person_count", "timestamp_us", "camera_id"],
  "params": {
    "person_class_id": "int",
    "helmet_class_id": "int",
    "min_overlap_x100": "int"
  },
  "rule": "For each person, take the top 30 percent of their box as the head region; if no detection of helmet_class_id overlaps that region by at least min_overlap_x100 percent, emit a violation for that track.",
  "skeleton": "ppe_compliance.txt"
}
