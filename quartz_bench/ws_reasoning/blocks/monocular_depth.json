{
  "name": "monocular_depth",
  "description": "Per-pixel RELATIVE depth map for the whole frame - not metric, not per-object.",
  "kind": "camera_event",
  "source": {
    "header": "ai_tasks/common/include/MonocularDepthResult.hpp",
    "cpp_result_type": "CMonocularDepthResult",
    "cpp_element_type": "SDepthMap",
    "record_container": "depth"
  },
  "cardinality": "single",
  "has_tracker": false,
  "accepts": ["image.full_frame"],
  "emits": "depth.map",
  "subtypes": [],
  "provides": ["width", "height", "min_val", "max_val", "map_b64"],
  "note": "map_b64 is a base64-encoded uint8 row-major buffer, one byte per pixel, normalised between min_val and max_val. No boxes, no ids - cannot drive any box-based or track-based block."
}
